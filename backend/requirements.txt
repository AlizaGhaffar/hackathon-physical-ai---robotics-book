# FastAPI framework and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Environment and configuration
python-dotenv>=1.0.0

# Database
# Using psycopg (v3) instead of psycopg2 for better Windows compatibility
psycopg[binary]>=3.1.13
asyncpg>=0.29.0

# AI and ML
openai>=1.12.0
qdrant-client>=1.16.1

# Authentication (better-auth integration via API)
pydantic>=2.6.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# HTTP and API
httpx>=0.26.0
aiohttp>=3.9.0

# Utilities
pydantic-settings>=2.1.0
